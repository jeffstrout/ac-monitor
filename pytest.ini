[pytest]
# Set the import path explicitly rather than relying on pytest's implicit
# rootdir insertion, which varies with invocation (`pytest` vs `python -m
# pytest`) and with whether a tests/conftest.py exists.
pythonpath = .
testpaths = tests
