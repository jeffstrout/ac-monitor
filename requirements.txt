# AC Monitor runtime dependencies (installed into the Docker image).
# Versions intentionally left loose during design; pin before first deploy.

paho-mqtt          # MQTT client + Home Assistant discovery
fastapi            # web dashboard + REST API (incl. /api/version)
uvicorn[standard]  # ASGI server for FastAPI
pyyaml             # config.yaml loading
# The HAT is read via the Sequent `ioplus` CLI (subprocess) — compiled into the
# Docker image from SequentMicrosystems/ioplus-rpi (see deploy/Dockerfile), not
# a pip package. Thermistors (AD1-AD4) are read as analog volts; conversion is
# in ac_monitor/hat.py.
